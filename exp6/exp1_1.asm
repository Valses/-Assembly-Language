.386
STACK  SEGMENT USE16 STACK
       DB 200 DUP(0)
STACK  ENDS

CODE   SEGMENT USE16
       ASSUME CS:CODE,SS:STACK

RINT	DW ?,?

NEWAB:	JMP DWORD PTR RINT
		CMP AL,41H
		JE AB
		CMP AL,42H
		JE BA
AB:     MOV AL,42H
		JMP OUT1
BA:		MOV AL,41H
OUT1:   IRET

START:  PUSH CS
		POP DS
		MOV AX,3516H
		INT 21H
		MOV RINT,BX
		MOV RINT+2,ES
		
		MOV DX,OFFSET NEWAB
		MOV AX,2516H
		INT 21H
		
		MOV DX,OFFSET START+15
		MOV CL,41H
		SHR DX,CL
		ADD DX,10H

		MOV AL,0
		MOV AH,31H
		INT 21H
		
CODE    ENDS
		END START
	
	   
